
services:
  app:
    container_name: spring-boot-app
    image: ${DOCKER_IMAGE:-local/yummypass:latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always